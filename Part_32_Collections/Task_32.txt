1) Для двусвязного списка добавить следующие методы:
- operator ++, которые добавляют к каждому элементу '!' (префик, посфикс)
- operator --, который удаляет одну букву в конце строки  (префик, посфикс)
- operator >>, << выводит на экран и в файл (друзья класса)

fstream stream;
stream << my_list;
stream.close();

2) Построить частотный словарь слов на основе одного заданного файла. Использовать резличные виды коллекций:
- vector
- list
- map
- unordered_map

Сортировать НЕ НАДО! Вычислить время работы в каждом отдельном случае до сохранения словарей в файл.